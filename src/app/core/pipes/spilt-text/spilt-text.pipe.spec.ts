import { SpiltTextPipe } from './spilt-text.pipe';

describe('SpiltTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SpiltTextPipe();
    expect(pipe).toBeTruthy();
  });
});
